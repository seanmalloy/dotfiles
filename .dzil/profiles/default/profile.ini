[TemplateModule/:DefaultModuleMaker]
template = Module.pm

[GenerateFile]
filename =  t/{{ $dist->name }}.t
is_template = 1
name_is_template = 1
content = use strict;
content = use warnings;
content = use Test::More tests => 1;
content = use Test::Exception;
content = BEGIN { use_ok('{{ join('::', split(/-/, $dist->name)) }}') };
content = 
content = my $Module = "{{ join('::', split(/-/, $dist->name)) }}";
content = 

[GatherDir::Template]
root = skel
include_dotfiles = 1

[Git::Init]
[GitHub::Create]
repo = {{ lc $dist -> name }}
has_wiki = 0

