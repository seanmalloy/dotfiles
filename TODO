1. Finish initial tmux config and setup.
   * See tm script
   * left and right status bar config
   * Requirements
     * Multi session ... think "cluster ssh"

2. Create deploy script.
   * self extracting shell script?
   * Pull current ack?
   * Pull VIM modules?

3. Detect if OpenSSH "ControlPersist" option
   is supported or not.

4. Perl Dist::Zilla configuration.

