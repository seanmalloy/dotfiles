1. Finish initial tmux config and setup.
   * See tm script
   * left and right status bar config
   * C-a P ssh -W vs nc
   * Multi session ... think "cluster ssh"

2. Update Install Script
   * Setup vim modules
   * Dynamically setup .gitconfig name
   * Dynamically setup .gitconfig email
   * Dynamically setup .ssh/config jump host

3. Detect if OpenSSH "ControlPersist" option
   is supported or not.

4. Perl Dist::Zilla configuration.

5. Vim modules.

